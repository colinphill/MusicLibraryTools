<ContentDialog
    x:Class="MusicLibraryManager.Dialogs.FieldsDialog"
    x:Name="DialogRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="All metadata fields"
    Width="820" MinWidth="720" MaxWidth="920"
    HorizontalContentAlignment="Stretch">
    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMinWidth">720</x:Double>
        <x:Double x:Key="ContentDialogMaxWidth">920</x:Double>
    </ContentDialog.Resources>
    <Grid Width="760" Height="620" MaxHeight="720" MinHeight="460" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Spacing="5">
            <TextBlock Text="{Binding Title}" Opacity="0.64" TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="Mixed values remain unchanged until you enter a replacement. Removed fields can be restored before saving."
                       Opacity="0.58" TextWrapping="Wrap"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="10">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Rows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,4" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold"
                                               TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Name}"/>
                                    <Grid Grid.Column="1">
                                        <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="Mixed values — type to replace" Margin="10,0" VerticalAlignment="Center"
                                                   Foreground="Gray" IsHitTestVisible="False"
                                                   Visibility="{Binding IsMixed, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                    </Grid>
                                    <Button Grid.Column="2" Content="{Binding RemoveButtonText}"
                                            Command="{Binding DataContext.RemoveFieldCommand, ElementName=DialogRoot}"
                                            CommandParameter="{Binding}" MinWidth="74"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <ProgressBar IsIndeterminate="True" Height="3" VerticalAlignment="Top"
                             Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="Add field" Opacity="0.68" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding AddableFields}" SelectedItem="{Binding FieldToAdd, Mode=TwoWay}"/>
            <Button Grid.Column="2" Content="Add" Command="{Binding AddFieldCommand}"/>
        </Grid>
        <Grid Grid.Row="3" ColumnSpacing="8">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="{StaticResource BrandAmberBrush}" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="Cancel" Command="{Binding CancelCommand}"/>
            <Button Grid.Column="2" Content="Save fields" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}"/>
        </Grid>
    </Grid>
</ContentDialog>
