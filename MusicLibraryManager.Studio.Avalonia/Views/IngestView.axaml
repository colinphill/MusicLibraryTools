<UserControl x:Class="MusicLibraryManager.Studio.Avalonia.Views.IngestView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:MusicLibraryManager.Studio.Avalonia.Controls"
             DragDrop.AllowDrop="True" DragDrop.DragOver="OnDragOver" DragDrop.Drop="OnDrop">
  <Grid Margin="26,22,26,26" RowDefinitions="Auto,Auto,*" RowSpacing="14">
    <controls:StudioPageHeader Title="Ingest"
        Subtitle="Preflight, preview, and safely import music into the active library.">
      <controls:StudioPageHeader.Actions>
        <StackPanel Orientation="Horizontal" Spacing="8">
          <Button Classes="studio" Content="Preflight" Command="{Binding PreflightCommand}" />
          <Button Classes="studio" Content="Preview" Command="{Binding PreviewCommand}" />
          <Button Classes="studio primary" Content="Apply" Command="{Binding ApplyCommand}" />
          <Button Classes="studio" Content="Cancel" Command="{Binding CancelCommand}" />
        </StackPanel>
      </controls:StudioPageHeader.Actions>
    </controls:StudioPageHeader>

    <Border Grid.Row="1" Classes="card">
      <StackPanel Spacing="10">
        <Grid ColumnDefinitions="*,Auto,220" ColumnSpacing="8">
          <StackPanel Spacing="5"><TextBlock Classes="muted" Text="Incoming folder" FontSize="12" FontWeight="SemiBold" /><TextBox Classes="studio" Text="{Binding SourceDirectory, Mode=TwoWay}" PlaceholderText="Drop a folder here or browse" /></StackPanel>
          <Button Grid.Column="1" Classes="studio" VerticalAlignment="Bottom" Content="Browse" Command="{Binding BrowseSourceCommand}" />
          <ComboBox Grid.Column="2" Classes="studio" VerticalAlignment="Bottom" ItemsSource="{Binding RecentSources}" SelectedItem="{Binding SelectedRecentSource, Mode=TwoWay}" PlaceholderText="Recent sources" />
        </Grid>
        <Grid ColumnDefinitions="*,220,Auto,Auto" ColumnSpacing="8">
          <StackPanel Spacing="5"><TextBlock Classes="muted" Text="Preset name" FontSize="12" FontWeight="SemiBold" /><TextBox Classes="studio" Text="{Binding PresetName, Mode=TwoWay}" /></StackPanel>
          <ComboBox Grid.Column="1" Classes="studio" VerticalAlignment="Bottom" ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset, Mode=TwoWay}" PlaceholderText="Presets" />
          <Button Grid.Column="2" Classes="studio" VerticalAlignment="Bottom" Content="Save preset" Command="{Binding SavePresetCommand}" />
          <Button Grid.Column="3" Classes="studio" VerticalAlignment="Bottom" Content="Delete" Command="{Binding DeletePresetCommand}" />
        </Grid>
        <Border Classes="status-banner"><TextBlock Text="{Binding ConfigurationReadinessText}" TextWrapping="Wrap" /></Border>
        <ItemsControl ItemsSource="{Binding PreflightChecks}" IsVisible="{Binding HasPreflightChecks}">
          <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate><DataTemplate><Border Classes="pill" Margin="0,0,8,6"><TextBlock Classes="muted" Text="{Binding Message}" /></Border></DataTemplate></ItemsControl.ItemTemplate>
        </ItemsControl>
        <ProgressBar Classes="studio" Minimum="0" Maximum="{Binding ApplyProgressMaximum}" Value="{Binding ApplyProgress}" IsVisible="{Binding IsApplying}" />
        <TextBlock Classes="muted" Text="{Binding StatusText}" TextWrapping="Wrap" />
      </StackPanel>
    </Border>

    <Border Grid.Row="2" Classes="card" Padding="12" ClipToBounds="True">
      <TabControl>
        <TabItem Header="Preview">
          <Grid RowDefinitions="Auto,*" RowSpacing="10" Margin="0,12,0,0">
            <Grid ColumnDefinitions="Auto,Auto,Auto,Auto,*,220" ColumnSpacing="8">
              <Border Classes="pill"><TextBlock Classes="muted" Text="{Binding AlbumCount, StringFormat={}{0} albums}" /></Border>
              <Border Grid.Column="1" Classes="pill"><TextBlock Classes="muted" Text="{Binding OutputCount, StringFormat={}{0} outputs}" /></Border>
              <Border Grid.Column="2" Classes="pill"><TextBlock Classes="muted" Text="{Binding ConflictCount, StringFormat={}{0} conflicts}" /></Border>
              <Border Grid.Column="3" Classes="pill"><TextBlock Classes="muted" Text="{Binding CleanupCount, StringFormat={}{0} cleanup}" /></Border>
              <ComboBox Grid.Column="5" Classes="studio" ItemsSource="{Binding PreviewFilters}" SelectedItem="{Binding SelectedPreviewFilter, Mode=TwoWay}" />
            </Grid>
            <controls:StudioDataGrid x:Name="PreviewGrid" Grid.Row="1" ItemsSource="{Binding Files}" />
          </Grid>
        </TabItem>
        <TabItem Header="History">
          <Grid RowDefinitions="Auto,*" RowSpacing="10" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" Spacing="8"><Button Classes="studio" Content="Refresh history" Command="{Binding RefreshHistoryCommand}" /><TextBlock Classes="muted" VerticalAlignment="Center" Text="{Binding HistoryStatus}" /></StackPanel>
            <ListBox Grid.Row="1" ItemsSource="{Binding History}" Background="Transparent" BorderThickness="0">
              <ListBox.ItemTemplate><DataTemplate><Border Classes="card" Padding="11" Margin="0,0,0,6"><StackPanel Spacing="3"><TextBlock Text="{Binding AffectedItems}" FontWeight="SemiBold" /><TextBlock Classes="muted" Text="{Binding State}" /><TextBlock Classes="faint" Text="{Binding RunPath}" TextTrimming="CharacterEllipsis" /></StackPanel></Border></DataTemplate></ListBox.ItemTemplate>
            </ListBox>
          </Grid>
        </TabItem>
      </TabControl>
    </Border>
  </Grid>
</UserControl>
